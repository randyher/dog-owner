Make a New Dog

<%= form_for @dog do |f| %> 

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :owner_id %>
    <%= f.collection_select :owner_id, Owner.all, :id, :name %> 

    <%= f.submit %>
    <% end %>